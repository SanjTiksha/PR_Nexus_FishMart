[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA fallback — required for React Router
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
